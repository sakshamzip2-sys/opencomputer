# {{ plugin_name }}

{{ description }}

A toolkit plugin for OpenComputer. Toolkits add new tools the agent
can invoke during a conversation (Read / Write / Bash / ...).

## Install

Copy this directory into one of:

- `~/.opencomputer/plugins/{{ plugin_id }}/` (global)
- `~/.opencomputer/profiles/<name>/plugins/{{ plugin_id }}/` (profile-local)

Or run `opencomputer plugin install <path-to-this-dir>`.

## Tools

- `{{ class_name }}` — replace with a real description.

## Authored by

{{ author }}
