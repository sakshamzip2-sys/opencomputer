"""Implementations of built-in slash commands.

Each module here defines exactly one :class:`plugin_sdk.SlashCommand`
subclass. The list of built-ins lives in
:mod:`opencomputer.agent.slash_commands` so adding a new one is a
two-line touch (drop the file in here, append the class in the
parent module's tuple).
"""
