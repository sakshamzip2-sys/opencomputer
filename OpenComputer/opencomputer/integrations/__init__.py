"""Bundled integration templates (langfuse, etc.).

Each subdirectory holds a self-contained service that OC can launch on
behalf of the user via the ``oc <integration>`` CLI surface (e.g.
``oc langfuse up``).
"""
