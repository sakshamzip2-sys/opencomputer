"""voice-mode plugin slash commands.

Single-command surface (``/voice``) — toggle voice-mode runtime state +
report status. Lives outside the agent core so the plugin is the source
of truth for its own controls.
"""
