{
  "id": "dingtalk",
  "name": "DingTalk Channel",
  "kind": "channel",
  "version": "0.1.0",
  "entry": "plugin",
  "description": "DingTalk (钉钉) outbound via Custom Robot webhook. Posts markdown messages to a group chat. Inbound message receive needs an outbound-callback URL hosted by you — deferred.",
  "single_instance": true,
  "enabled_by_default": false,
  "setup": {
    "channels": [
      {
        "id": "dingtalk",
        "env_vars": ["DINGTALK_WEBHOOK_URL", "DINGTALK_SECRET"],
        "label": "DingTalk (钉钉 — Custom Robot webhook)",
        "signup_url": "https://open.dingtalk.com/document/robots/custom-robot-access"
      }
    ]
  }
}
